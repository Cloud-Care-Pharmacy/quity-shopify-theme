<!-- customers/register.liquid -->
<style>
  .register-hero {
    background-color: {{ settings.color_body_bg | default: '#ffffff' }};
    min-height: 600px;
    position: relative;
    display: flex;
    align-items: center;
  }
  .register-hero-inner {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    width: 100%;
    padding: 60px 0;
  }
  .register-left {
    flex: 1;
    min-width: 300px;
    padding-right: 60px;
    color: {{ settings.color_body_headings | default: '#222222' }};
  }
  .register-left h1 {
    font-size: 42px;
    font-weight: 400;
    margin-bottom: 14px;
    letter-spacing: -0.5px;
    line-height: 1.2;
    font-family: var(--g-heading-font-family, inherit);
    color: {{ settings.color_body_headings | default: '#222222' }};
  }
  .register-left .register-subtitle {
    font-size: 16px;
    color: {{ settings.color_body_text | default: '#414141' }};
    margin-bottom: 36px;
    line-height: 1.7;
    font-family: var(--g-body-font-family, inherit);
    opacity: 0.7;
  }
  .register-benefits {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .register-benefits li {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 14px 0;
    font-size: 15px;
    color: {{ settings.color_body_text | default: '#414141' }};
    border-bottom: 1px solid rgba(0,0,0,0.06);
    font-family: var(--g-body-font-family, inherit);
  }
  .register-benefits li:last-child {
    border-bottom: none;
  }
  .register-benefits .benefit-icon {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: #f5f5f5;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .register-benefits .benefit-icon svg {
    width: 18px;
    height: 18px;
    fill: none;
    stroke: {{ settings.color_body_headings | default: '#222222' }};
    stroke-width: 2.5;
    stroke-linecap: round;
    stroke-linejoin: round;
  }
  .register-right {
    flex: 1;
    min-width: 340px;
    max-width: 480px;
  }
  .register-card {
    background: #f9f9f9;
    border: 1px solid rgba(0,0,0,0.06);
    border-radius: 16px;
    padding: 40px 36px;
  }
  .register-card .form-group {
    margin-bottom: 16px;
  }
  .register-card .form-control {
    background: #ffffff;
    border: 1px solid rgba(0,0,0,0.1);
    border-radius: 10px;
    color: {{ settings.color_body_headings | default: '#222222' }};
    padding: 14px 18px;
    font-size: 15px;
    font-family: var(--g-body-font-family, inherit);
    transition: all 0.2s ease;
  }
  .register-card .form-control::placeholder {
    color: rgba(0,0,0,0.35);
  }
  .register-card .form-control:focus {
    border-color: {{ settings.color_body_headings | default: '#222222' }};
    box-shadow: 0 0 0 3px rgba(0,0,0,0.05);
    outline: none;
  }
  .register-card .btn-register {
    width: 100%;
    padding: 14px;
    background: {{ settings.color_body_headings | default: '#222222' }};
    color: {{ settings.color_body_bg | default: '#ffffff' }};
    border: none;
    border-radius: 10px;
    font-size: 15px;
    font-weight: 500;
    font-family: var(--g-body-font-family, inherit);
    cursor: pointer;
    transition: all 0.2s ease;
    margin-top: 8px;
    text-transform: uppercase;
    letter-spacing: 1px;
  }
  .register-card .btn-register:hover {
    opacity: 0.85;
    transform: translateY(-1px);
    box-shadow: 0 4px 15px rgba(0,0,0,0.15);
  }
  .register-card .register-footer {
    text-align: center;
    margin-top: 20px;
    font-size: 14px;
    color: {{ settings.color_body_text | default: '#414141' }};
    font-family: var(--g-body-font-family, inherit);
    opacity: 0.6;
  }
  .register-card .register-footer a {
    color: {{ settings.color_body_headings | default: '#222222' }};
    text-decoration: underline;
  }
  .register-card .register-footer a:hover {
    opacity: 0.7;
  }
  @media (max-width: 768px) {
    .register-hero { min-height: auto; }
    .register-hero-inner { padding: 40px 0; }
    .register-left { padding-right: 0; margin-bottom: 40px; }
    .register-left h1 { font-size: 32px; }
    .register-right { max-width: 100%; }
    .register-card { padding: 30px 24px; }
  }
</style>

<section class="register-hero">
  <div class="page-width">
    <div class="register-hero-inner">
      <div class="register-left">
        <h1>Get Started<br>With Us</h1>
        <p class="register-subtitle">Create your account to access our full range of pharmacy services. A valid prescription is needed to browse and purchase products.</p>
        <ul class="register-benefits">
          <li>
            <span class="benefit-icon">
              <svg viewBox="0 0 24 24"><path d="M9 12l2 2 4-4"/><circle cx="12" cy="12" r="10"/></svg>
            </span>
            Verified Prescription Management
          </li>
          <li>
            <span class="benefit-icon">
              <svg viewBox="0 0 24 24"><path d="M3 3h18v18H3z"/><path d="M9 9h6v6H9z"/></svg>
            </span>
            Fast &amp; Convenient Ordering
          </li>
          <li>
            <span class="benefit-icon">
              <svg viewBox="0 0 24 24"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
            </span>
            Your Privacy, Fully Protected
          </li>
          <li>
            <span class="benefit-icon">
              <svg viewBox="0 0 24 24"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
            </span>
            Dedicated Care Team
          </li>
        </ul>
      </div>
      <div class="register-right">
        <div class="register-card">
          <div class="form-vertical">
            {%- form 'create_customer' -%}
            {{ form.errors | default_errors }}
            <div class="form-group">
              <input class="form-control" type="text" name="customer[first_name]" id="FirstName" placeholder="{{ 'customer.register.first_name' | t }}" {% if form.first_name %}value="{{ form.first_name }}"{% endif %}>
              <label for="FirstName" class="label--hidden">{{ 'customer.register.first_name' | t }}</label>
            </div>
            <div class="form-group">
              <input class="form-control" type="text" name="customer[last_name]" id="LastName" placeholder="{{ 'customer.register.last_name' | t }}" {% if form.last_name %}value="{{ form.last_name }}"{% endif %}>
              <label for="LastName" class="label--hidden">{{ 'customer.register.last_name' | t }}</label>
            </div>
            <div class="form-group">
              <input class="form-control" type="email" name="customer[email]" id="Email" placeholder="{{ 'customer.register.email' | t }}" {% if form.email %} value="{{ form.email }}"{% endif %} autocorrect="off" autocapitalize="off">
              <label for="Email" class="label--hidden">{{ 'customer.register.email' | t }}</label>
            </div>
            <div class="form-group">
              <input class="form-control" type="password" name="customer[password]" id="CreatePassword" placeholder="{{ 'customer.register.password' | t }}">
              <label for="CreatePassword" class="label--hidden">{{ 'customer.register.password' | t }}</label>
            </div>
            <button type="submit" class="btn-register">{{ 'customer.register.submit' | t }}</button>
            <div class="register-footer">
              <span>{{ 'customer.register.already' | t }}</span> <a href="/account/login">{{ 'customer.register.login' | t }}</a>
            </div>
            {%- endform -%}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>